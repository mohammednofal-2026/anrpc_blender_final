name: anrpc_blender
description: A new Flutter project for ANRPC.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  screenshot: ^2.1.0
  path_provider: ^2.1.1
  image_gallery_saver: ^2.0.3
  permission_handler: ^11.0.1 # إضافة مهمة للأندرويد

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
